
    // 1) //
    let square1 = 4;
    
    let squareS = square1 * 4;
    console.log(`Периметр квадрата: ${squareS} см2`);

    // 2) //

    let cube1 = 5;

    let cubeV = cube1 ** 3;
    console.log(`Объем куба: ${cubeV} см3`);

    let cubesideS = 4 * (cube1 ** 2);
    console.log(`Площадь боковой поврехности куба: ${cubesideS} см2`)

    // 3) //

    let circleR = 6;
    const pi = 3.14;

    let circumferens = circleR * pi * 2;
    console.log(`Длина окружноти: ${circumferens} см`);

    let circleS = pi * (circleR ** 2);
    console.log(`Площадь круга: ${circleS} см2`);

    // 4) //

    let mass = 270;
    let Volume = 1;

    let density = mass / Volume;
    console.log(`Плотность вещества: ${density} кг/м3`);

    // 5) //

    let population = 127000000;
    let territory = 377000;

    let populterr = population / territory;

    let populdens = Math.ceil(populterr);
    
    console.log(`Плотность населения: ${populdens} жителей на км2`);

    // 6) //

    let trianA = 4;
    let trianB = 6;

    let trianC = Math.sqrt (trianA ** 2 + trianB ** 2);
    console.log(`Длина гипотенузы прямоугольного треугольника: ${trianC} см`);

    // 7) //

    let aa = 4;

    let yy = ((aa ** 2) + 10) / (Math.sqrt ((aa ** 2) + 1));
    console.log(`Значение "y": ${yy}`);









